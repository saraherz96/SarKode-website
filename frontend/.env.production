# Vite carga este archivo automáticamente al correr `vite build` (modo producción).
# Reemplaza con la URL real que te asigne Render tras el deploy
# (Dashboard → tu servicio → arriba a la izquierda, algo como https://sarkode-backend.onrender.com),
# o con tu dominio propio si luego configuras uno custom (ej. https://api.sarkode.com).
VITE_API_URL=https://sarkode-backend.onrender.com
